<%= form_for [:organization_admin, @file_group] do |f| %>
  <% if @file_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@file_group.errors.count, "error") %> prohibited this file_group from being saved:</h2>

      <ul>
      <% @file_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label t('group.name') %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label t('group.description') %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label t('group.position') %><br>
    <%= f.number_field :position %>
  </div>
  <div class="actions">
    <%= f.submit t('group.submit')%>
  </div>
<% end %>
